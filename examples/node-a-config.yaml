# SpaceComms Node A Configuration
# Operator Alpha

node:
  id: "node-alpha"
  name: "Operator Alpha Ground Station"

server:
  host: "0.0.0.0"
  port: 8080

logging:
  level: "info"
  format: "pretty"

storage:
  storage_type: "memory"

protocol:
  heartbeat_interval_seconds: 30
  session_timeout_seconds: 120
  max_hop_count: 10

# No preconfigured peers - will add via API
peers: []
